jobs:
  
  checkov:
    machine: true
    steps:
      - checkout

      - run: docker run --rm -w /src -v $(pwd):/src bridgecrew/checkov -d aws -o json | tee checkov-output.json

      - store_artifacts:
          path: checkov-output.json
          destination: checkov-artifact
  
  
  tflint:
    machine: true
    steps:
      - checkout

      - run: 
          command: docker run --rm -v $(pwd):/data -t ghcr.io/terraform-linters/tflint aws -f json | tee tflint-output.json
          when: on_fail             # Even if the job fails, continue to the next stages

      - store_artifacts:
          path: tflint-output.json
          destination: tflint-artifact
          when: always

  test:
    machine: true
    steps:
      - checkout
      - run: echo "This is a test step"

  integration:
    machine: true
    steps:
      - checkout
      - run:
          command: |
            echo "This is an integration step"
            exit 1

  prod:
    machine: true
    steps:
      - checkout
      - run: echo "This is a deploy step"

workflows:
  version: 2
  terraform:
    jobs:
      - checkov
      - tflint
          requires:
            - checkov
      - test:
          requires:
            - tflint
      - integration:
          requires:
            - test
      - prod:
          type: approval
          requires:
            - integration
